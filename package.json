{
  "name": "am-i-average",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "esbuild scripts/check.ts --bundle --platform=node --format=esm --outfile=node_modules/.check.mjs --loader:.css=empty --log-level=error && node node_modules/.check.mjs",
    "shots": "node scripts/shots.mjs",
    "test:ai": "node scripts/check-resolver.mjs",
    "stub": "node scripts/stub-resolver.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}